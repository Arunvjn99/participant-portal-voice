# ── Core AI Backend ──
# The Gemini API key lives in server/.env (never in the frontend).
# See server/.env.example for backend configuration.

# ── Optional: Legacy LLM settings (deprecated) ──
# VITE_LLM_API_ENDPOINT=/api/llm
# VITE_LLM_ENABLED=false
