# Core AI Voice Assistant & LLM

# Optional: Override LLM API base URL (default: /api/llm for Netlify)
# VITE_LLM_API_ENDPOINT=/api/llm

# Optional: Enable LLM enhancement (normalize spoken numbers, polish responses)
# Set to "true" to enable. Default: false
# VITE_LLM_ENABLED=false

# Optional: Gemini API key (for geminiClient if used)
# VITE_GEMINI_API_KEY=
